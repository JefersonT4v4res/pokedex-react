import styled from "styled-components"

export const StyledMain = styled.main`
    
    width: calc(100% - 50px);
    height: 90vh;
    padding: 1rem;
    margin: 1rem auto;
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    box-sizing: border-box ;

`