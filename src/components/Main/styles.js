import styled from "styled-components"

export const StyledMain = styled.main`
    
    width: calc(100% - 10px);
    height: 100vh;
    padding: 1rem;
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    box-sizing: border-box ;

`